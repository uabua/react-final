import React from 'react';
import Navigation from './navigation';
import Logo from './logo';

const Header = () => {
    return <header>
        <Logo />
        <Navigation /> 
    </header>;
}

export default Header;